const ButtonBorrowCancel = (props) => {
    const { title } = props;

    return (
        <div className="borrow_create_cancel">
            <h4 className="text-white text-center search">{title} </h4>
        </div>
    );
};

export default ButtonBorrowCancel;
