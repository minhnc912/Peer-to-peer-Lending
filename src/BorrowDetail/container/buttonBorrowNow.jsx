const ButtonBorrowNow = (props) => {
    const { title } = props;

    return (
        <div className="body-search">
            <h4 className="text-white text-center search">{title} </h4>
        </div>
    );
};

export default ButtonBorrowNow;
